const header = document.querySelector('.display-3');
const desc = document.querySelector('.lead');
const label = document.querySelector('label');
const example = document.querySelector('#example');
const lngButton = document.querySelector('#lng');
const addButton = document.querySelector('#addRSS');

export { lngButton, addButton, header, desc, label, example };
